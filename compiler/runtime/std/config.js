/**
 * Ljos Configuration Helper
 * Provides defineConfig for type-safe configuration
 */

/**
 * Helper function for defining Ljos configuration
 * This is a pass-through function that provides type hints in IDEs
 * @param {Object} config - The configuration object
 * @returns {Object} The same configuration object
 */
export function defineConfig(config) {
  return config;
}

# Test function return type error

fn getFloat(): Float {
    return 3.14
}

fn wrongUsage() {
    # Should fail: Float cannot be assigned to Int (no implicit conversion)
    const x: Int = getFloat()
}
